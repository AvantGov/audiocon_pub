// * DATA 
import { photos_ids } from '../../assets/data/photos_ids';

export const getImgIds = (num) => {
    // console.log("you're getting this gallery:", photos_ids[num])
    return photos_ids[num]
};